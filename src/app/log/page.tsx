import { SectionPage } from '@/components/site/section-page'

export default function Page() {
  return <SectionPage eyebrow='BUILD LOG / EVENTS' title='A visible history' copy='Milestones, deployments and field notes will arrive here as a chronological public event surface.' />
}
